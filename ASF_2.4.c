// Implimenting Knuth-Morris-Pratt algorithm for pattren matching search
#include <stdio.h>
void KMP(char *str,char *wrd){
	 int slen
}
int main(void){
	char str[10000];
	char substr[10000];
	printf("Enter string: ");
	gets(str);
	printf("Enter substring to be matched: ");
	gets(substr);
	printf("KMP results are:\n");
	KMP(str,wrd)
}
